#include<conio.h>
#include<stdio.h>

int main()
{
    int a , b ;

        printf("Enter two numbers: ");
        scanf("%d %d", &a , &b);

            int difference = a - b;
        printf("The difference is %d", difference);
return 0;
}